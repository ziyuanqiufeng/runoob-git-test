# 《问道长生》系统功能总览

> 汇总时间：2026-09-16（架构数字更新于 09-22，engine 拆分十期收官后）
> 定位：PySide6 桌面单机修仙模拟器，AI（Agnes LLM `agnes-3.0-flash` + 文生图 `agnes-image-2.1-flash`）驱动动态剧情与立绘。
> 测试基线：1197 用例全绿；性能：单月结算 0.02ms。

---

## 一、代码与内容规模（实测）

| 维度 | 数量 |
|---|---|
| Python 源文件 | 1056 个 / 约 32.9 万行（含测试） |
| 引擎层 `game/` | 88 文件 / 2.4 万行（GameEngine 门面 1633 行 + 11 个域 Mixin 文件 + 52 个 Manager） |
| 界面层 `ui/` | 77 文件 / 1.8 万行（75+ 个对话框/面板） |
| 测试层 `tests/` | 116 文件 / 1.9 万行，**1170 用例** |
| 工具层 `tools/` | 24 文件 |
| 配置 `config/` | 61 个 JSON |
| 功能开关 | 15 个 |
| 境界 | 18 级（练气→筑基→金丹→元婴…） |
| 地点 | 28 处（8 城 / 10 宗 / 10 野） |
| 随机事件 | 35 个（地点差异化权重，28×35 全覆盖） |
| 物品 | 63 种 |
| NPC | 101 个 |
| 技能 | 151 个 |
| 流派 | 10 条（法/体/剑/邪/丹/器/兽/魂/阵/符） |
| 宗门 | 10 个 |
| 结局 | 8 种 |

---

## 二、核心玩法

### 1. 修炼与境界
- 18 级境界渐进解锁玩法；灵根（五行 + 雷冰风变异/融合）决定属性与神通。
- 突破大境界需**渡劫**（雷劫 + 心魔考验），失败可能走火入魔。
- 元婴期解锁**神识扫描**（洞察妖兽弱点）与**替死机制**（一次离体挡死）。

### 2. 游历与世界
- 28 地点各有性格：城池市井（人际/商贩高发）、宗门清净（修炼/走火高发）、野外凶险（资源/战斗高发）。
- 35 个随机事件按地点权重抽取，奇遇/危机/人际/资源/特殊触发五类。
- 世界 BOSS、世界事件、天气/灵气潮汐驱动长线节奏。

### 3. 战斗系统
- 回合制 + 五行相克 + 控制效果（盾/封/晕/反伤）。
- 151 技能带熟练度成长（`mastery_effects`）；敌人有境界缩放、特殊技能、召唤机制。

### 4. 经营与势力
- 洞府租赁/闭关、摆摊、拍卖、坊市；领地建设/产出/防御。
- 10 宗门（外门→掌门晋升）、城主竞选与政策、家族系统（联姻/继承）。

### 5. 长线机制
- 心魔/心境/道心/信念四维状态；天道注视与降罚。
- 寿元轮回转世；红尘羁绊；百家争鸣。
- **8 结局**：飞升成仙 / 魔道登天 / 大道独行 / 走火入魔 / 天道降罚 / 兵解重修 / 红尘归隐 / 凡尘谢幕。

### 6. 主线剧情
- 章节制推进，按境界/宗门/结局条件解锁，全部完成后走完“问道长生之路”。

---

## 三、AI 能力（Agnes 系，同一 key）

| 模型 | 用途 | 状态 |
|---|---|---|
| `agnes-3.0-flash`（LLM） | 游历动态剧情：按地点+心境生成专属古风文案 | 已接入 `api.agnes-ai.cn`，实测 1~7.5s/次 |
| `agnes-image-2.1-flash`（文生图） | AI 立绘生成 + 捏脸部件 | 已接入，503 自动重试 |

**两段式 AI 剧情**：离线文案秒渲染（界面零阻塞）→ 后台线程生成 AI 文案 → 以「✨ AI 剧情」追加日志。失败自动降级离线模板。

**AI 捏脸（双路线）**：
- AI 提示词：5 维特征（脸型/眼神/气质/发型/服饰）→ 整张水墨立绘（约 1 分钟）。
- 拼装：25 个 AI 水墨部件 × 3 肤色，PIL 实时合成，参数可复现，🎲 随机一键。

**玩家自助**：
- 功能开关（15 项）设置面板可视化切换。
- 「测试 AI 连接」一键诊断 key/网络/LLM+图模型。
- 游戏中头像右键随时**重捏脸**。

---

## 四、存档与数据安全

- 多槽位 + **原子写入**（tmp + os.replace，断电不损档）。
- **滚动备份**：`saves/backups/` 保留 5 份。
- **版本迁移**：旧存档自动 `_migrate`。
- **导出/导入**：zip 分享包（含自定义立绘），导入自动修复立绘路径。
- API key 等敏感配置已 gitignore，不入 GitHub。

---

## 五、质量保障

- **1197 测试用例**（单元/集成/UI offscreen），覆盖事件内容完整性、抽取可达性、捏脸、导出导入、AI 异步化等。
- 配置驱动：数值/事件/开关尽量 JSON 化，调平衡不改代码。
- 月度结算插座（`register_monthly_tick`）+ 单系统异常隔离。
- 性能基准：单月 0.02ms / 存档 4.5ms / 事件抽取 9.6µs（`tools/bench_tick.py`）。
- 曲线复测工具：`tools/curve_check.py`（纯闭关口径实测各境界停留时长）。

---

## 六、玩家入口速查

| 入口 | 功能 |
|---|---|
| 登录页 | 继续上次 / 选择存档（载入/删除/导出/导入） |
| 角色创建 | 捏脸 → 灵根觉醒 → 流派/难度选择 |
| 状态面板 | 头像右键=捏脸/换立绘；详情展开任务与灵根 |
| 操作面板 | 闭关/游历/宗门/家族/领地/坊市/拍卖/图鉴等（随境界解锁） |
| 设置菜单 | 音量 / 功能开关 / 测试 AI 连接 / 难度 |
| 帮助菜单 | 玩法指南 / 修行年谱（19 处埋点） |
| 日志面板 | 系统消息 + 「✨ AI 剧情」紫字 |

---

## 七、已知待改进（2026-09-22 更新）

> 历史改进全部闭环（9/16-22）：事件去重 ✅ 主线引导 ✅ 导入立绘校验 ✅ 平衡报告 ✅
> **修为曲线方案①已实施**（cultivate 乘境界系数 1+order×0.8，筑基 4.25 年落回设计意图，
> 见 `docs/balance_report_20260916.md`）✅ **engine 拆分十期收官**（6216→1633 行，-73.7%，
> 12 个域 Mixin，含九期纯净度修正）✅。

**当前遗留：**
1. 健康度复扫（9/22，拆分收官后）：1197 用例全绿、12 Mixin 零同名冲突、61 配置 JSON 全部可加载、balance_sim 稳健性通过——**无结构性技术债**。
2. 剩余均为体验/数值层打磨（玩法手感、AI 文案质量、数值细调）。

> 详细设计意图见 `docs/game_design_overview.md` 与 `docs/gdd/` 系列；玩家向说明见 `docs/玩家手册.md`。
